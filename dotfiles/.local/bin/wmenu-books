#!/bin/sh


BOOKS="$(xdg-user-dir DOCUMENTS)/books"
CHOOICE=$(find $BOOKS -exec basename {} \; | tail -n +2 | wmenu -f 'LigaMonaco Nerd Font 12' -N 000000 -n ffffff -i -l 5)
[ -n "$CHOOICE" ] && zathura "$BOOKS/$CHOOICE"
