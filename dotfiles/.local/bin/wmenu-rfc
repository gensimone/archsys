#!/bin/sh

rfc="/usr/share/doc/rfc/pdf"

# tail -n +2 discard the first element of the list.
selected=$(find $rfc -exec basename {} \; | tail -n +2 | wmenu "$@" -l 5)

[ -n "$selected" ] && zathura "$rfc/$selected"
