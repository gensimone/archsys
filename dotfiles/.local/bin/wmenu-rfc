#!/bin/sh

rfc="/usr/share/doc/rfc/txt"

# tail -n +2 discard the first element of the list.
selected=$(ls $rfc | tail -n +2 | wmenu "$@" -l 5)

[ -n "$selected" ] && foot --app-id=rfc less $rfc/$selected
