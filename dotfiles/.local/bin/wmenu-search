#!/bin/sh

sel=`printf "" | wmenu "$@" -p "Search:"`
[ -z "$sel" ] && exit 0

if printf "$sel" | grep -E "^[[:alnum:]]+\.[[:alnum:]]+$"; then
    chromium "$sel" >/dev/null 2>&1
else
    chromium "duckduckgo.com/?q=$sel" >/dev/null 2>&1
fi
